{% comment %}
  jamones-quality-tiers.liquid
  Sistema de categorías con 4 medallones: Oro, Plata, Bronce III·a, Bronce III·b
  Inspirado en sellos editoriales y medallones gastronómicos clásicos.
{% endcomment %}

<style>
  .qt-{{ section.id }} {
    background: var(--cm-off);
    padding: 56px 72px 64px;
  }

  /* ── Header ── */
  .qt__header-{{ section.id }} {
    text-align: center;
    margin-bottom: 48px;
  }

  .qt__eyebrow-{{ section.id }} {
    display: block;
    font-family: var(--cm-font-sans);
    font-size: 11px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--cm-text-light);
    margin-bottom: 16px;
  }

  .qt__heading-{{ section.id }} {
    font-family: var(--cm-font-serif);
    font-size: clamp(24px, 2.8vw, 36px);
    font-weight: 400;
    color: var(--cm-navy);
    line-height: 1.2;
  }

  /* ── Grid de 4 columnas ── */
  .qt__grid-{{ section.id }} {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 32px;
    max-width: 1400px;
    margin: 0 auto;
  }

  /* ── Medallón ── */
  .qt__medallion-{{ section.id }} {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    text-decoration: none;
    cursor: pointer;
    transition: transform 0.3s ease, filter 0.3s ease;
  }

  .qt__medallion-{{ section.id }}:hover {
    transform: translateY(-8px);
  }

  .qt__medallion-{{ section.id }}:focus-visible {
    outline: 2px solid var(--cm-gold);
    outline-offset: 8px;
  }

  /* ── Escudo redondeado ── */
  .qt__shield-{{ section.id }} {
    position: relative;
    width: 140px;
    height: 160px;
    border-radius: 16px 16px 24px 24px;
    background: var(--cm-white);
    margin-bottom: 20px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    border: 2px solid;
  }

  /* Colores de borde según tier */
  .qt__shield-gold-{{ section.id }} { border-color: #C4973A; }
  .qt__shield-silver-{{ section.id }} { border-color: #A8A8A8; }
  .qt__shield-bronze-{{ section.id }} { border-color: #A07040; }

  /* ── Título "JAMÓN IBÉRICO" dentro del escudo ── */
  .qt__shield-title-{{ section.id }} {
    font-family: var(--cm-font-serif);
    font-size: 8px;
    font-style: italic;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--cm-text-light);
    margin-bottom: 12px;
    line-height: 1;
  }

  /* ── Número romano dentro del escudo ── */
  .qt__shield-numeral-{{ section.id }} {
    font-family: var(--cm-font-serif);
    font-size: 36px;
    font-weight: 400;
    line-height: 1;
    margin-bottom: 8px;
  }

  .qt__shield-gold-{{ section.id }} .qt__shield-numeral-{{ section.id }} { color: #C4973A; }
  .qt__shield-silver-{{ section.id }} .qt__shield-numeral-{{ section.id }} { color: #A8A8A8; }
  .qt__shield-bronze-{{ section.id }} .qt__shield-numeral-{{ section.id }} { color: #A07040; }

  /* ── Detalle inferior (punto o espiga) ── */
  .qt__shield-detail-{{ section.id }} {
    font-size: 12px;
    height: 12px;
  }

  /* ── Contenedor de textos bajo el escudo ── */
  .qt__content-{{ section.id }} {
    flex: 1;
  }

  .qt__category-label-{{ section.id }} {
    font-family: var(--cm-font-sans);
    font-size: 8px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--cm-text-light);
    margin-bottom: 8px;
  }

  .qt__category-name-{{ section.id }} {
    font-family: var(--cm-font-serif);
    font-size: 20px;
    font-weight: 400;
    color: var(--cm-navy);
    line-height: 1.3;
    margin-bottom: 8px;
  }

  .qt__category-desc-{{ section.id }} {
    font-family: var(--cm-font-sans);
    font-size: 13px;
    color: var(--cm-text);
    line-height: 1.5;
  }

  /* ── Tablet ── */
  @media (max-width: 1023px) {
    .qt-{{ section.id }} {
      padding: 40px 40px 48px;
    }
    .qt__header-{{ section.id }} {
      margin-bottom: 36px;
    }
    .qt__grid-{{ section.id }} {
      grid-template-columns: repeat(2, 1fr);
      gap: 24px;
    }
    .qt__shield-{{ section.id }} {
      width: 120px;
      height: 140px;
    }
    .qt__shield-numeral-{{ section.id }} {
      font-size: 28px;
    }
    .qt__category-name-{{ section.id }} {
      font-size: 18px;
    }
  }

  /* ── Mobile ── */
  @media (max-width: 767px) {
    .qt-{{ section.id }} {
      padding: 32px 24px 40px;
    }
    .qt__header-{{ section.id }} {
      margin-bottom: 28px;
    }
    .qt__heading-{{ section.id }} {
      font-size: 24px;
    }
    .qt__grid-{{ section.id }} {
      grid-template-columns: repeat(2, 1fr);
      gap: 20px;
    }
    .qt__medallion-{{ section.id }} {
      margin: 0 auto;
    }
    .qt__shield-{{ section.id }} {
      width: 100px;
      height: 120px;
    }
    .qt__shield-numeral-{{ section.id }} {
      font-size: 24px;
    }
    .qt__category-name-{{ section.id }} {
      font-size: 16px;
    }
    .qt__category-desc-{{ section.id }} {
      font-size: 12px;
    }
  }
</style>

<section class="qt-{{ section.id }}" id="qt-section-{{ section.id }}">
  <div class="qt__header-{{ section.id }}">
    {%- if section.settings.eyebrow != blank -%}
      <span class="qt__eyebrow-{{ section.id }}">{{ section.settings.eyebrow }}</span>
    {%- endif -%}
    {%- if section.settings.heading != blank -%}
      <h2 class="qt__heading-{{ section.id }}">{{ section.settings.heading }}</h2>
    {%- endif -%}
  </div>

  <div class="qt__grid-{{ section.id }}">
    {%- assign tier_colors = 'gold,silver,bronze,bronze' | split: ',' -%}
    {%- assign numerals = 'I,II,III,III' | split: ',' -%}
    {%- assign level_suffixes = '','','·a','·b' | split: ',' -%}
    {%- assign details = '','','•','🌾' | split: ',' -%}

    {%- for i in (0..3) -%}
      {%- assign color_class = tier_colors[i] -%}
      {%- assign numeral = numerals[i] -%}
      {%- assign suffix = level_suffixes[i] -%}
      {%- assign detail = details[i] -%}
      {%- assign settings_key = 'cat' | append: i | append: '_name' -%}
      {%- assign settings_desc = 'cat' | append: i | append: '_desc' -%}

      <a
        class="qt__medallion-{{ section.id }}"
        href="#cat-{{ i }}"
        aria-label="Ver detalles de {{ section.settings[settings_key] }}"
      >
        <div class="qt__shield-{{ section.id }} qt__shield-{{ color_class }}-{{ section.id }}">
          <div class="qt__shield-title-{{ section.id }}">Jamón Ibérico</div>
          <div class="qt__shield-numeral-{{ section.id }}">{{ numeral }}</div>
          {%- if detail != '' -%}
            <div class="qt__shield-detail-{{ section.id }}">{{ detail }}</div>
          {%- endif -%}
        </div>

        <div class="qt__content-{{ section.id }}">
          <div class="qt__category-label-{{ section.id }}">Categoría</div>
          <h3 class="qt__category-name-{{ section.id }}">
            {{ section.settings[settings_key] }}{%- if suffix != '' -%} {{ suffix }}{%- endif -%}
          </h3>
          <p class="qt__category-desc-{{ section.id }}">{{ section.settings[settings_desc] }}</p>
        </div>
      </a>
    {%- endfor -%}
  </div>
</section>

{% schema %}
{
  "name": "Jamones — Sistema de Categorías",
  "tag": "section",
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Etiqueta superior",
      "default": "Sistema de Categorías"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Titular",
      "default": "Cuatro piezas, tres niveles"
    },
    {
      "type": "header",
      "content": "🥇 Categoría I — Oro"
    },
    {
      "type": "text",
      "id": "cat0_name",
      "label": "Nombre",
      "default": "Oro"
    },
    {
      "type": "textarea",
      "id": "cat0_desc",
      "label": "Descripción",
      "default": "Máxima calidad. Bellota, curación extendida."
    },
    {
      "type": "header",
      "content": "🥈 Categoría II — Plata"
    },
    {
      "type": "text",
      "id": "cat1_name",
      "label": "Nombre",
      "default": "Plata"
    },
    {
      "type": "textarea",
      "id": "cat1_desc",
      "label": "Descripción",
      "default": "Calidad media-alta. Cebo de campo selecto."
    },
    {
      "type": "header",
      "content": "🥉 Categoría III·a — Bronce (Expresión sobria)"
    },
    {
      "type": "text",
      "id": "cat2_name",
      "label": "Nombre",
      "default": "Bronce III·a"
    },
    {
      "type": "textarea",
      "id": "cat2_desc",
      "label": "Descripción",
      "default": "Calidad base, expresión sobria."
    },
    {
      "type": "header",
      "content": "🥉 Categoría III·b — Bronce (Cebo de cereal)"
    },
    {
      "type": "text",
      "id": "cat3_name",
      "label": "Nombre",
      "default": "Bronce III·b"
    },
    {
      "type": "textarea",
      "id": "cat3_desc",
      "label": "Descripción",
      "default": "Misma calidad. Cebo de cereal selecto."
    }
  ],
  "presets": [
    {
      "name": "Jamones — Sistema de Categorías"
    }
  ]
}
{% endschema %}
