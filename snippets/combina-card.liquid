{% comment %}
  combina-card.liquid
  Variables esperadas:
  - cp : producto Shopify
  - cp_variant : variante del producto
{% endcomment %}
<div class="cm-combina__card">
  <a href="{{ cp.url }}" class="cm-combina__card-link">
    <div class="cm-combina__card-img-wrap">
      {%- if cp.featured_image -%}
        <img
          src="{{ cp.featured_image | image_url: width: 400 }}"
          alt="{{ cp.title | escape }}"
          class="cm-combina__card-img"
          loading="lazy"
          width="400"
          height="400"
        >
      {%- endif -%}
    </div>
  </a>
  <div class="cm-combina__card-body">
    <a href="{{ cp.url }}" class="cm-combina__card-title">{{ cp.title | truncate: 50 }}</a>
    <div class="cm-combina__card-foot">
      <p class="cm-combina__card-price">{{ cp.price | money }}</p>
      {%- if cp_variant -%}
        <button
          class="cm-combina__card-plus"
          data-variant-id="{{ cp_variant.id }}"
          aria-label="Añadir {{ cp.title | escape }} a la cesta"
          type="button"
        >
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
            <line x1="12" y1="5" x2="12" y2="19"/>
            <line x1="5" y1="12" x2="19" y2="12"/>
          </svg>
        </button>
      {%- endif -%}
    </div>
  </div>
</div>
